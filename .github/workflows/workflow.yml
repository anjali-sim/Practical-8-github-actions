name: React CI

on:
  push:
    branches:
      - "feature"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [15.x]

    steps:
      - uses: actions/chechout@v1
      - run: npm install
      - run: npm run build
      - run: npm test